services:
  - type: web
    name: speaker-identifier
    runtime: python
    buildCommand: "apt-get install -y ffmpeg && pip install -r requirements.txt && python preload_model.py"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"